module alu_shift8 (
    input op [4],
    input a [8],
    input b [8],
    output c [8]
  ) {
  
  sig result [4][8];

  always {
    result = {$signed(a) >>> b, $signed(a) <<< b, a >> b, a << b};
    
    // choose shift direction and type from opcode
    c = result[op[2:0]];
  }
}
