module alu_cmp_zvn (
    input op [4],
    input z,
    input v,
    input n,
    output c [8]
  ) {
  
  always {
    // mask results for lt and eq with opcode
    // reduce with OR to check if any result is true
    c = |(op[2:1] & c{n^v, z});
  }
}
